syntax = "proto3";

package product;

message Product {
    string id = 1;
    string name = 2;
    float price = 3;
}

service Products {
    rpc GetProducts(Empty) returns (ProductList){}
    rpc GetProductById(ProductId) returns (Product) {}
    rpc CreateProduct(Create) returns (Product) {}
    rpc DeleteProductById(ProductId) returns (Product) {}
}

message Create {
    string name = 1;
    float price = 2;
}

message ProductId {
    string id = 1;
}

message Empty {}

message ProductList {
    repeated Product product = 1;
}
